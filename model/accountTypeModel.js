const db = require('../config/database');

const AccountType = {
  account_types_id: 'account_types_id',
  account_types_name: 'account_types_name',
  account_types_image: 'account_types_image'
};

module.exports = AccountType;
